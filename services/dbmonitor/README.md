Disk monitor for the database. This service must be co-resident with the database mongod daemon.
It periodically reads the disk partition and updates a collection - which can be reported back 
to the Admin.
